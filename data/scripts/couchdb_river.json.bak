{
  "type" : "couchdb",
  "couchdb" : {
    "host" : "'"${DB_HOST}"'",
    "port" : "'"${DB_PORT}"'",
    "db" : "'"${DB_NAME}"'",
    "script": "ctx._type = ctx.doc.type",
    "user": "'"${DB_ADMIN_USER}"'",
    "password" : "'"${DB_ADMIN_PASSWORD}"'"
  },
  "index" : {
    "index" : "'"${DB_NAME}"'",
    "bulk_size" : "100",
    "bulk_timeout" : "10ms"
  }
}
